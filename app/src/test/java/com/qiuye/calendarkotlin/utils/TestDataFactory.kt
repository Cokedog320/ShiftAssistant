package com.qiuye.calendarkotlin.utils

/**
 * 测试数据工厂。
 * 用于在测试中提供标准、可复用的默认数据对象，减少样板代码。
 */
object TestDataFactory {
    // TODO: 在这里添加各种数据类的工厂方法
    // 例如：
    // fun createMockTask(id: String = "1", title: String = "Test") = Task(id, title)
}
